from django.urls import path

from . import views

app_name = "inventory"

urlpatterns = [
    # Dashboard sub-views land here on Day 2 (low-stock drill-down,
    # can-make view, draft-PO-from-low-stock action).
]
